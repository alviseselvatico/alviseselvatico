# Speech fixture (D016)

Place here ONE operator-owned speech recording (`sample_en.wav`, <= 90 s, mono, 16 kHz, English).
It is the only media committed to the repository and is registered as source S001 with an `OWNED` policy (`can_publish=false`).

Until it exists, transcription tests must be skipped with an explicit reason, not faked.
